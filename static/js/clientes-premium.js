(function(){const input=document.getElementById('clientSearch');const list=document.getElementById('clientsList');const empty=document.getElementById('clientsNoResults');document.querySelectorAll('.client-progress span[data-progress]').forEach(function(bar){const value=Math.max(0,Math.min(100,Number(bar.dataset.progress||0)));bar.style.width=value+'%';});if(!input||!list)return;const rows=Array.from(list.querySelectorAll('.client-row'));function normalize(value){return String(value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();}function filter(){const term=normalize(input.value);let visible=0;rows.forEach(function(row){const show=!term||normalize(row.dataset.search).includes(term);row.hidden=!show;if(show)visible+=1;});if(empty)empty.hidden=visible!==0;}input.addEventListener('input',filter);})();
